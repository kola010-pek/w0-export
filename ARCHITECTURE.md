# 系统架构

## 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    负责人工作台 (Frontend)                  │
│  ┌─────┐ ┌──────┐ ┌─────┐ ┌──────┐ ┌──────┐ ┌──────┐  │
│  │总览 │ │Agent │ │ DAG │ │质量  │ │模型  │ │审批  │    │
│  └──┬──┘ └──┬───┘ └──┬──┘ └──┬───┘ └──┬───┘ └──┬───┘  │
└─────┼───────┼────────┼───────┼────────┼────────┼───────┘
      │       │        │       │        │        │
      └───────┴────────┴───────┴────────┴────────┘
                              │
                    ┌─────────┴─────────┐
                    │   API Routes       │
                    │  (Next.js Server)  │
                    └─────────┬─────────┘
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
   ┌──────┴──────┐   ┌───────┴───────┐   ┌──────┴──────┐
   │ Agent Engine │   │  DAG Engine   │   │ Mock Tools  │
   │ (权限校验)   │   │  (状态机)     │   │ (模拟执行)   │
   └──────┬──────┘   └───────┬───────┘   └──────┬──────┘
          │                   │                   │
          └───────────────────┼───────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │   State Store     │
                    │  (JSON 持久化)    │
                    └───────────────────┘
```

## 核心组件

### 1. Agent Engine（权限引擎）

负责验证每个 Agent 的操作是否在其权限范围内：
- 工具白名单校验
- 禁止动作校验
- 生产环境访问控制

### 2. DAG Engine（状态机）

管理任务的确定性执行流程：
- 按拓扑排序执行节点
- 检查前置依赖
- 评估门禁状态
- 处理 BLOCK 传播
- 管理审批等待

### 3. Mock Tools（模拟工具层）

提供模拟的工具执行：
- 数据更新模拟
- 质量检查模拟
- 模型运行模拟
- 发布模拟
- 所有结果标记 `mock: true`

### 4. State Store（状态存储）

持久化所有运行状态：
- Run 记录
- Task 记录
- Gate 结果
- Approval 记录
- Audit 事件

## 数据流

```
负责人发起任务
    │
    ▼
总调度官创建 Run
    │
    ▼
DAG 引擎按拓扑序派发任务
    │
    ▼
Agent Engine 校验权限
    │
    ▼
Mock Tools 执行工具
    │
    ▼
结果写入 Store
    │
    ▼
门禁评估
    │
    ├── PASS → 继续下游
    ├── WARN → 人工审批
    └── BLOCK → 下游全部 SKIPPED_BY_GATE
```
