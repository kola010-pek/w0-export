# PILOT-001 Test Commands
# All commands executed during construction verification.

## TypeScript Check
pnpm ts-check
# Result: Errors found ONLY in pre-existing files (tests/integration/w0-integration.test.ts, tests/smoke/w0-smoke.test.ts).
# No errors in authorized files (phase2/page.tsx, pilot-001-governance-card.spec.ts).
# Pre-existing errors are NOT introduced by PILOT-001 changes.

## ESLint Check
pnpm lint
# Result: Warnings and errors found ONLY in pre-existing files.
# No warnings or errors in authorized files (phase2/page.tsx, pilot-001-governance-card.spec.ts).
# Pre-existing issues are NOT introduced by PILOT-001 changes.

## Playwright Test
npx playwright test --config=/tmp/pw-pilot001.config.ts --reporter=list
# Note: Temporary config required because existing playwright.config.ts
# has testMatch restricted to '**/phase2-2-browser-tests.spec.ts'.
# The temporary config was created in /tmp (not in project directory)
# and does not modify any project file.
# Result: 8 passed (3.6s)
